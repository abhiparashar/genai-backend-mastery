# Optional/heavy deps. Install only when you reach the module that needs them.
# Every import of these in this repo is guarded, and the matching tests SKIP if absent,
# so the suite stays green without them.
#
#   pip install -r requirements-optional.txt

# --- Real providers (needed only for `-m live` tests) ---
openai>=1.30
anthropic>=0.25

# --- Tokenization: exact token counts + cost math ---
tiktoken>=0.7

# --- Vector stores ---
chromadb>=0.5             # embedded dev vector DB
# pgvector>=0.2           # Postgres extension client - prefer this in production
# qdrant-client>=1.9

# --- Local embeddings (no API cost, runs on CPU) ---
sentence-transformers>=3.0

# --- Document parsing for RAG ingestion ---
pypdf>=4.2
pdfplumber>=0.11          # better for tables/layout

# --- Orchestration frameworks (learn raw APIs FIRST, then these) ---
langchain>=0.2
langchain-community>=0.2
langgraph>=0.1
llama-index>=0.10

# --- Observability ---
langfuse>=2.36
opentelemetry-sdk>=1.25

# --- Production infra clients ---
redis>=5.0
sqlalchemy>=2.0
asyncpg>=0.29
